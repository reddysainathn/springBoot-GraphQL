schema {
    query: Query
}
type Query {
    mongoAllBooks: [Book]
    mongoBook(id: String): Book
}

type Book {
    isn: String
    title: String
    publisher: String
    authors: [String]
    publishedDate: String
}

